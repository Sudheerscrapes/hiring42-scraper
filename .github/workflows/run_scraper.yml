jobs:
run:
runs-on: ubuntu-latest

steps:
  - uses: actions/checkout@v4

  - uses: actions/setup-python@v5
    with:
      python-version: "3.11"

  - run: pip install playwright

  - run: python -m playwright install chromium

  - run: python hiring42_scraper1.py
```
